package com.onezero.launcher.launcher.callback;

import com.onezero.launcher.launcher.appInfo.AppInfo;

public interface AppIconClickCallBack {
    void IconClick(AppInfo info);
    void IconLongClick(AppInfo info);
}
